class AppFonts {
  const AppFonts._();

  /// Primary UI typeface (bundled under assets/fonts).
  static const String primary = 'Satoshi';

  /// Alias kept for older call sites that referenced a secondary family.
  static const String secondary = primary;
}
